# lin34.github.io
Personal website 2021

# To configure a build for deployment
in angular.json, the build command was configured to output to the docs/ folder.
Github should recognize this folder as the one to deploy the website on.

steps:
ng build

using git:
add all files, commit and push.